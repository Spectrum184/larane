import Layout from '~/components/Layout';
import React, { FC } from 'react';

const AnnotationContainer: FC = () => {
  return <Layout>AnnotationContainer</Layout>;
};

export default AnnotationContainer;
